{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/api/hero-list", "dest": "/api/hero-list.ts" },
    { "src": "/api/hero-detail/(?<id>.*)", "dest": "/api/hero-detail/[id].ts" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
